<view class="right-banner-list">
  <view class="banner-item" onTap="clickBanner" data-type="{{item.type}}" a:for="{{ rightBannerList }}">
    <icon type="{{item.icon}}" class="custom-size" style="font-size: 80rpx" />
    <view>
      {{item.text}}
    </view>
  </view>
</view>